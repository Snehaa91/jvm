package InterviewPractice;

public interface Teach {
	
	int i = 0; //public static final (by default)
	
	public abstract void run(); //Only abstract method until 7th verion of java

	// 8th version -> public + default and static methods are also created
	// 9th version -> public + private method can also be written.

}
